@model List<AccountManagement.Application.Contract.RejectionReason.RejectionReasonViewModel>
@{
        ViewData["Title"] = "مدیریت دلایل رد درخواست ارتقا حساب کاربری";
}

<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-header">
                <button type="button" class="btn btn-rounded btn-info" onclick="window.location.href ='#showmodal=/administrator/Mysetting/_RejectionReasonCreate'">
                    <span class="btn-icon-left text-info">
                        <i class="fa fa-plus color-info"></i>
                    </span>افزودن
                </button>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-responsive-md">
                     <thead>
                            <tr>
                                <th>عنوان دلیل</th>
                                <th>عملیات</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.Title</td>
                                    <td>
                                        <div class="d-flex">
                                            <a href="#showmodal=/administrator/MySetting/_RejectionReasonEdit/@item.Id" class="btn btn-primary shadow btn-xs sharp mr-1"><i class="fa fa-pencil"></i></a>
                                            <a href="#" class="btn btn-danger shadow btn-xs sharp"><i class="fa fa-trash"></i></a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
